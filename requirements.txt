# CIS Audit Tool - Dependencies
#
# tkinter is included with standard Python (no pip install needed)
#
# Report generation
xhtml2pdf>=0.2.11     # HTML-to-PDF conversion without GTK dependencies
jinja2>=3.1.0         # HTML report templating
#
# Audit helpers
psutil>=5.9.0         # Cross-platform system info
